import { useState, useRef } from "react";
import { StreamHistoryItem, SessionChat, StreamEvent } from "@/lib/types";
import { parseSseBlock } from "@/lib/utils";

export function useSessionAnalysis() {
    // maybe we can extract the whole state here?
}
